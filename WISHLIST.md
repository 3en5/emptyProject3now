# WISHLIST.md — קובץ משאלות

מקום לרשום רעיונות עתידיים, שיפורים וחלומות שכרגע **לא** מתעסקים בהם.
המטרה — לא לשכוח רעיונות טובים, בלי שהם ייכנסו לפרויקט או להתקדמות שלו.

> **חשוב — זה לא backlog.** הפריטים כאן אינם מתוכננים ואינם מתועדפים.
> כשמחליטים באמת לפתח משהו מכאן — מעבירים אותו ל-`PROGRESS.md` (Backlog).
> לרשום כאן בחופשיות, בלי מחשבה על ישימות או סדר.

---

## 💡 רעיונות (Ideas)

| # | הרעיון | קטגוריה | הערות / הקשר |
|---|--------|---------|---------------|
| ~~1~~ | ~~**העלאה חכמה עם זיהוי אוטומטי של מסמכים**~~ | ✅ מומש במלואו | חילוץ+סיווג+תיוק+פיקוח (תיבת הקליטה) + **מנוע היברידי: כללים מקומיים ונפילה ל-Claude ראייה לסרוקים/עברית**. ראה PROGRESS #28, #43, #47 |
| ~~2~~ | ~~**השוואת שנה-לשנה + התראה על מסמכים חסרים**~~ | ✅ מומש | `/api/comparison/:year` + עמוד השוואת שנים. ראה PROGRESS #27 |
| 3 | **קליטה → רישום גוף חדש + הוספה אוטומטית ל"מה צריך להעלות בשנה הבאה"** | 💭 בבירור | בעת גרירת מסמך: (א) אם הגוף המנפיק לא קיים ברשימת הגופים — יירשם שם אוטומטית (לא רק "ממתין לשיוך" גנרי); (ב) שם המסמך שזוהה יתווסף אוטומטית כ"צפוי" גם לשנה הבאה. המשתמש מגבש את הרעיון — ראה פירוט #3 למטה |

---

## 📄 פירוט משאלה #1 — העלאה חכמה עם זיהוי אוטומטי

**הרעיון:** להעלות קובץ (PDF/תמונה), והמערכת מזהה לבד את מאפייניו — סוג המסמך, עבור איזה שירות הוא יצא, ומי הגוף שהנפיק אותו — נותנת ממשק להשוואה-לתקן ולתיקון, ושומרת את המסמך במקום הנכון.

**סקיצת יישום (pipeline בן 4 שלבים):**

1. **חילוץ טקסט** — רוב המסמכים הם PDF דיגיטלי עם שכבת טקסט → `pdf-parse`/`pdfjs` (מהיר, חינם). מסמך סרוק → OCR עברית (שלב מתקדם, קשה יותר).
2. **סיווג** — מה/ממי/לאיזה שירות:
   - *מבוסס-כללים:* מילון "טביעות אצבע" בטקסט (שמות גופים, "867", "טופס 106", ח.פ.). יעיל כי רשימת הגופים סופית וידועה.
   - *מבוסס-LLM (Claude + structured output):* מחזיר `{docType, issuer, service, year}`. מבין עברית, גמיש לפורמטים חדשים.
   - *היברידי (מומלץ):* כללים קודם, נפילה ל-LLM למה שלא זוהה.
3. **מיפוי + השוואה לתקן** — התאמה ל-`financial_entity` קיים (fuzzy match), והצלבה מול `REPORTS.md`: "זה 867 של מיטב 2025 → המסמך שחיכינו לו". בדיקת אי-התאמות (גוף/שנה/חוסר).
4. **אישור/תיקון + שמירה** — טופס עם שדות ממולאים מראש; המשתמש מאשר/מתקן; שמירת הקובץ + מטא-דאטה ב-DB (`documents.file_path` כבר קיים), וסימון פריט המשימה כ"הושג".

**מסלול מומלץ:** MVP מבוסס-כללים (בלי תלות חיצונית) → הוספת שכבת Claude לדיוק וגמישות.

**תלויות/פתוחות:** ספריית חילוץ PDF, אחסון קבצים, OCR עברית לסרוקים, החלטה על שימוש ב-Claude API (עלות מול דיוק).

---

## 📅 פירוט משאלה #2 — השוואת שנה-לשנה + התראה על חוסרים

**הרעיון:** המערכת משווה שנה קודמת לשנה נוכחית ומתריעה אילו מסמכים חסרים — על סמך מה שהיא **מצפה** לקבל לפי מה שהתקבל אשתקד. כולל אפשרות לסמן שהתקשרות עם גוף **הסתיימה** (לא לצפות ממנו יותר) או **התחילה** (להתחיל לצפות).

**סקיצת יישום:**

1. **"סט מצופה" לשנה** נגזר משני מקורות:
   - *גלגול (carry-forward):* כל מסמך שהתקבל בשנה N‑1 → נוצר כ"מצופה" לשנה N באותו גוף.
   - *קטלוג (`REPORTS.md`):* לפי סוג הגוף, אילו דוחות סטנדרטיים אמורים להגיע.
2. **מנוע הפרש (diff):** `מצופה − התקבל בפועל = חסר` → זו רשימת ההתראות.
3. **ניהול תחילת/סוף התקשרות** (המפתח):
   - להוסיף ל-`financial_entities` שדות תאריך: `active_from` / `active_until` (שדה `status` כבר קיים).
   - *הסתיימה:* מסמנים `active_until` → מהשנה הבאה הגוף יוצא מהסט המצופה; ההיסטוריה נשמרת.
   - *חדשה:* גוף עם `active_from` → נכנס לסט המצופה מאותה שנה.
4. **תצוגה בתחילת שנה:** 🔴 חסר · 🟢 התקבל · ➕ חדש · ⏹️ הסתיים.

**ההשפעה:** רשימת המשימות השנתית מפסיקה להיות מילוי ידני — המערכת **מרכיבה אותה לבד** מההיסטוריה.

**תלויות/פתוחות:** שינוי סכימה (`active_from`/`active_until`), מנוע יצירת "סט מצופה" שנתי, הגדרת חוקי גלגול (אילו סוגי מסמכים מתגלגלים אוטומטית ואילו לא). מתחבר ישירות למשאלה #1 (מסמך שמזוהה → מסמן "התקבל") ולקטלוג `REPORTS.md`.

---

## 🏢📆 פירוט משאלה #3 — רישום גוף אוטומטי + הוספה ל"מצופה" לשנה הבאה

**הרעיון (כפי שהועלה, עדיין בבירור מול המשתמש):** בעת גרירת מסמך שהגוף המנפיק שלו מזוהה (למשל "מגדל"):
1. אם הגוף עדיין לא קיים ברשימת הגופים הפיננסיים — **נרשם אוטומטית** (במקום להיכנס ל"📥 ממתין לשיוך" הגנרי, או בנוסף לזה).
2. שם המסמך שזוהה (הסוג/הזיהוי הספציפי) **מתווסף אוטומטית לרשימת המסמכים שצריך להעלות גם בשנה הבאה** — כלומר נכנס ל"סט המצופה" השנתי, בלי שהמשתמש יצטרך להוסיף אותו ידנית למשימות/checklist.

**שאלות פתוחות שעוד לא סוכמו (לחכות להמשך המשתמש לפני מימוש):**
- כשגוף נרשם אוטומטית — עם אילו שדות (`type`/`category`)? יש כבר `suggestedType` מהזיהוי (ראה משאלה #1), אבל צריך להחליט אם זה נרשם *בלי אישור אנושי בכלל*, בסתירה לעיקרון "זיהוי+אישור" שנשמר בכל שאר המערכת (entity matching, checklist matching, owner matching) — ראה `CLAUDE.md`.
- "הוספה לשנה הבאה" — האם זה חופף למשאלה #2 (השוואת שנה-לשנה, `active_from`/`active_until`, גלגול-קדימה של מסמכים שהתקבלו)? ייתכן שזה בעצם אותו מנגנון, רק שמופעל *מיד בקליטה* במקום בתחילת השנה.
- האם "מסמך חדש מסוג לא מוכר לגוף קיים" גם נכנס לסט המצופה, או רק דוחות מוכרים (867 וכו')?

**תלויות:** מתחבר ישירות למשאלה #2 (שדות `active_from`/`active_until`, מנוע "סט מצופה") ולעיקרון "detect + confirm" הקיים בכל המערכת.

---

## 📝 איך משתמשים

- **להוסיף:** כל רעיון שעולה — לזרוק לטבלה למעלה, גם אם הוא לא גמור.
- **לקדם:** כשמחליטים לפתח רעיון — להעביר אותו ל-Backlog ב-`PROGRESS.md`.
- **להסיר:** רעיון שכבר לא רלוונטי — למחוק בלי חשש.

---

## 🗂️ קטגוריות מוצעות (למיון עתידי)

כשהרשימה תגדל, אפשר לקבץ לפי:
- **פיצ'רים** — יכולות חדשות למשתמש
- **אוטומציה** — שליפה/עדכון אוטומטי
- **UX/עיצוב** — שיפורי חוויה ומראה
- **אינטגרציות** — חיבור לגופים חיצוניים
- **דוחות וניתוח** — תובנות ופלטים
