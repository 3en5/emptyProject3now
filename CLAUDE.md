# CLAUDE.md — כללי עבודה על הפרויקט

קובץ זה מגדיר את הכללים והמוסכמות של העבודה על הפרויקט. Claude קורא אותו אוטומטית בתחילת כל שיחה.

---

## 🎯 על הפרויקט

**מערכת ניהול מסמכים פיננסיים** — web app לניהול ומעקב אחר כל המסמכים והנכסים הפיננסיים של משק בית:
- חשבונות בנק (עסקי, משפחתי, השקעות)
- ביטוחים (חיים, בריאות, פנסיה, ביטוח מנהלים)
- השקעות (IBKR, IBI, BTB, מיטב דש)
- נדל"ן ומשכנתאות
- קרנות השתלמות, גמל, פוליסות חיסכון

**המטרה:** לדעת אילו דוחות צריך להגיש, לאן, ומה חסר — עם דגש על דוחות שנתיים (867 וכו'), תנועות בנק ומעקב ניירות ערך.

**שלבי פיתוח:**
1. שלב נוכחי — שימוש שנתי (סידור וארגון)
2. עתידי — שימוש חודשי (עדכונים שוטפים)
3. עתידי — שליפה אוטומטית של מסמכים מהגופים

---

## 🛠️ Tech Stack

- **Backend:** Node.js + Express (ESM modules, `"type": "module"`)
- **Database:** SQLite דרך `sql.js` (pure JavaScript — לא native bindings)
- **Frontend:** React 18 + Vite
- **HTTP client:** Axios
- **שפה:** ממשק בעברית, RTL

---

## 🗺️ מפת קבצים

לפני עריכת קבצים, לעיין ב-`INDEX.md` — הוא מתעד כל קובץ ותפקידו, וכולל חתך "לפי נושא" שמראה אילו קבצים מעורבים בכל שינוי. **בכל הוספה/מחיקה/שינוי-תפקיד של קובץ — לעדכן גם את `INDEX.md`.**

## 📊 מעקב התקדמות

בתחילת עבודה — לקרוא את `PROGRESS.md` כדי לדעת מה כבר נעשה, מה בעבודה ומה מתוכנן (למניעת עבודה כפולה והתנגשויות). **בתחילת משימה להוסיף אותה ל"בעבודה עכשיו", ובסופה להעביר ל"הושלם".**

## 📁 מבנה הפרויקט

```
backend/
├── db/
│   ├── schema.sql      # הגדרת הטבלאות
│   ├── init.js         # אתחול DB + שמירה לקובץ
│   └── helper.js       # runQuery / getOne / getAll
├── routes/             # entities, accounts, documents, checklists
└── server.js           # Express server (port 3001)

frontend/
├── src/
│   ├── components/     # Dashboard, Navigation, EntityForm, EntityList
│   ├── pages/          # EntitiesPage, ChecklistPage, DocumentPage
│   └── App.jsx         # ניהול state ראשי + קריאות API
└── vite.config.js      # port 5173, proxy ל-/api
```

---

## 🚀 הרצה

```bash
npm start                    # Backend על port 3001
cd frontend && npm run dev   # Frontend על port 5173
```

ה-Frontend פונה ל-`/api` דרך proxy של Vite — לא URL מוחלט.

---

## 📐 מוסכמות קוד

- **גישה ל-DB:** תמיד דרך `runQuery` / `getOne` / `getAll` מ-`backend/db/helper.js`. לא לגשת ל-`db.exec` ישירות ב-routes.
- **שמירת DB:** `sql.js` עובד בזיכרון — כל כתיבה (`runQuery`) קוראת ל-`saveDatabase()` שמסנכרנת לקובץ. לא לשכוח.
- **טיפול בשגיאות:** כל route עוטף ב-try/catch ומחזיר `{ error: ... }` עם status מתאים.
- **פרמטרים:** תמיד prepared statements עם `?` — לעולם לא string interpolation ל-SQL.
- **ID:** להמיר עם `parseInt()` לפני שאילתה.
- **עברית ו-RTL:** טקסט למשתמש בעברית, `dir="rtl"`. שמות משתנים/פונקציות/טבלאות באנגלית.

---

## 🌿 כללי Git

- **ענף עבודה:** `claude/hebrew-text-query-uzdtx6` — לפתח, לעשות commit ולדחוף רק לשם.
- **לעולם לא** לדחוף לענף אחר בלי אישור מפורש.
- **push:** תמיד `git push -u origin <branch>`.
- **commits:** הודעות ברורות ותיאוריות. commit על כל יחידת עבודה הגיונית.
- **לא ליצור PR** אלא אם ביקשתי במפורש.

---

## ✅ כללי עבודה כלליים

- **תחילה ייעוץ, אחר כך קוד** — כשמדובר במשהו לא טריוויאלי, קודם לתכנן ולהתייעץ, ואז לבנות.
- **לבדוק לפני שמסמנים "בוצע"** — להריץ ולוודא שהדבר עובד בפועל. לא לטעון שמשהו עובד בלי לבדוק.
- **לדווח בכנות** — אם בדיקה נכשלה, לומר זאת עם הפלט. אם שלב דולג, לציין.
- **שינויים הפיכים בזהירות** — לפני מחיקה/דריסה של קבצים, לבדוק מה יש שם.
- **סכימת DB:** שינוי ב-`schema.sql` דורש התייחסות ל-DB קיים (הטבלאות נוצרות עם `IF NOT EXISTS`, אז עמודה חדשה לא תיווצר אוטומטית בקובץ קיים).

---

## 🔮 כיוונים עתידיים (לזכור בתכנון)

- Upload מסמכים (PDF/תמונות) + OCR
- דוחות וניתוח נתונים (סיכומים שנתיים, השוואות)
- אינטגרציה עם API של בנקים לשליפה אוטומטית
- הרשאת read-only לבן/בת זוג
