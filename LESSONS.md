# LESSONS.md — קובץ לקחים

תיעוד תקלות מורכבות ופתרונות מורכבים, כדי שבפעם הבאה שנתפעל או נשכלל אותם — יהיו לנו הלקחים בהישג יד.

> **מתי לרשום כאן:** אחרי כל תקלה שדרשה יותר מתיקון טריוויאלי, כל פתרון לא-אינטואיטיבי, וכל "מלכודת" שקל ליפול בה שוב.
> **פורמט:** תסמין → סיבת שורש → פתרון → לקח לעתיד.

---

## לקח #1 — SQLite עם native bindings לא עובד בסביבה הזו

**תאריך:** 2026-07-08
**חומרה:** גבוהה (חסם את כל ה-backend)

### התסמין
בהרצת השרת עם `better-sqlite3` (ואחר כך גם `sqlite3`) התקבלה שגיאה:
```
Error: Could not locate the bindings file. Tried:
 → .../better-sqlite3/build/Release/better_sqlite3.node
 → ... (עוד עשרות נתיבים)
```

### סיבת השורש
שתי הספריות (`better-sqlite3`, `sqlite3`) הן **native modules** — הן דורשות קימפול של קוד C++ ל-`.node` binding מול גרסת ה-Node הספציפית. בסביבת ההרצה הזו הקימפול לא הצליח / ה-binary לא תואם, ולכן ה-binding לא נטען. החלפה בין שתי הספריות לא עזרה כי הבעיה משותפת לשתיהן.

### הפתרון
מעבר ל-**`sql.js`** — מימוש SQLite ב-WebAssembly / pure JavaScript, בלי native bindings כלל. עובד בכל סביבה.

המחיר וההשלכות:
- ה-DB חי **בזיכרון**; צריך לטעון מקובץ בהתחלה ולשמור לקובץ אחרי כל כתיבה.
- ה-API שונה: אין `db.prepare().get()/all()`. משתמשים ב-`db.exec()` / `db.run()`.
- לכן נבנתה שכבת עטיפה ב-`backend/db/helper.js` (`runQuery` / `getOne` / `getAll`) שממירה תוצאות ל-objects ומטפלת בשמירה.

### הלקח לעתיד
- **בסביבה הזו — להימנע מ-native modules.** להעדיף ספריות pure-JS / WASM.
- אם צריך DB אמיתי (Postgres/SQLite native) בעתיד — לוודא קודם שהקימפול עובד בסביבת ההרצה, לפני שבונים סביבו.
- כל גישה ל-DB חייבת לעבור דרך `helper.js`, ולזכור ש**כתיבה בלי `saveDatabase()` הולכת לאיבוד** בהפעלה הבאה.

---

## לקח #2 — sql.js לא יודע לקשור `undefined`

**תאריך:** 2026-07-08
**חומרה:** גבוהה (כל POST/INSERT נכשל)

### התסמין
כל יצירת ישות דרך ה-API החזירה `{}` (או `500`), ובלוג הופיע:
```
Query error: undefined
Insert failed: undefined
```

### סיבת השורש
שתי סיבות שהצטברו:
1. **`undefined` בפרמטרים:** בקשות ה-API שולחות רק חלק מהשדות; היתר מגיעים כ-`undefined`. sql.js יודע לקשור `null`/מספר/מחרוזת — אבל **זורק על `undefined`**. לכן ה-INSERT נכשל.
2. **תהליך שרת ישן:** בזמן הדיבוג רץ ברקע תהליך `node` עם קוד ישן, כך שתיקונים לכאורה "לא עבדו". חובה להרוג את כל התהליכים (`pkill -9 -f backend/server.js`) לפני בדיקה מחדש.

### הפתרון
פונקציית `sanitize()` ב-`backend/db/helper.js` שממירה כל `undefined` ל-`null` לפני קשירה, ומופעלת ב-`runQuery`/`getOne`/`getAll`.

### הלקח לעתיד
- **לעולם לא להעביר `undefined` ל-sql.js.** כל פרמטר עובר `sanitize` ב-helper — לכן להמשיך לגשת ל-DB רק דרכו.
- בסביבת background — לוודא שאין תהליך שרת ישן לפני שמסיקים שתיקון נכשל.
- להשתמש ב-`run_in_background` הייעודי של הכלי (ולא `&`), כי תהליכי `&` לא שורדים בין קריאות.

---

## לקח #3 — הטמעת פירמידת טסטים (gotchas)

**תאריך:** 2026-07-08
**חומרה:** בינונית (חסם הרצת טסטים עד לפתרון)

### התסמין / האתגרים
1. `node --test backend/test` נכשל: `Cannot find module '.../backend/test'` — ניסה לטעון תיקייה כקובץ.
2. אי אפשר לבדוק את ה-API בלי להריץ שרת אמיתי, וה-DB היה singleton עם נתיב קשיח.
3. Playwright/`@playwright/test` לא הוריד דפדפן (מנוטרל בסביבה) — צריך להצביע ל-chromium מותקן.

### הפתרון
1. **`node --test` דורש glob, לא תיקייה:** `node --test 'backend/test/**/*.test.js'`.
2. **בידוד DB:** `init.js` קורא `process.env.FINANCE_DB_PATH`; הערך `:memory:` = DB בזיכרון בלי כתיבה לדיסק. הטסטים מציבים אותו לפני ה-import.
3. **הפרדת app מ-server:** `backend/app.js` מייצא `createApp()` (בלי listen), כדי שטסטים יריצו בקשות עם supertest בלי פורט. `server.js` רק מייבא, מאתחל ומאזין.
4. **E2E:** `playwright.config.js` עם `webServer` שמפעיל backend (DB זרוע) + frontend, ו-`launchOptions.executablePath` ל-`/opt/pw-browsers/chromium-*/chrome-linux/chrome`.

### הלקח לעתיד
- טסטי backend חדשים → `backend/test/*.test.js`, מייבאים `createApp` ומציבים `FINANCE_DB_PATH=:memory:` **לפני** ה-import.
- לא לשבור את הפרדת `app.js`/`server.js` — היא מה שמאפשרת את הטסטים.
- להריץ `npm run test:all` לפני סיום פיצ'ר.

---

## לקח #4 — הוספת עמודה ל-DB קיים (מיגרציה) ב-sql.js

**תאריך:** 2026-07-08
**חומרה:** נמוכה (אבל שקט-מסוכן — עלול להיכשל בשקט על DB קיים)

### התסמין / הסיכון
הטבלאות נוצרות עם `CREATE TABLE IF NOT EXISTS`. כשמוסיפים עמודה ל-`schema.sql` (למשל `documents.year`), **DB קיים לא מקבל אותה** — ה-`CREATE` מדולג כי הטבלה כבר קיימת, ושאילתות על העמודה החדשה ייכשלו.

### הפתרון
פונקציית `ensureColumn(table, column, defn)` ב-`init.js` שרצה אחרי יצירת הסכימה: בודקת `PRAGMA table_info(...)` ומריצה `ALTER TABLE ADD COLUMN` רק אם העמודה חסרה. נוסף גם ל-`schema.sql` (ל-DB חדש) וגם כמיגרציה (ל-DB קיים).

### הלקח לעתיד
- **כל עמודה חדשה = לעדכן גם `schema.sql` וגם `ensureColumn` ב-`init.js`.** אחרת זה יעבוד אצל מי שמחק את ה-DB, וייכשל אצל מי ששמר אותו.
- לבדוק מיגרציה על DB קיים (לא רק על טרי) לפני שמסמנים "בוצע".

---

## לקח #5 — רענון נתונים שמפיל מסך "טוען" מוחק state מקומי של רכיבים

**תאריך:** 2026-07-08
**חומרה:** בינונית (הפיצ'ר "עבד" בבדיקות רכיבים ונשבר רק ב-E2E)

### התסמין
תיבת הקליטה החכמה העלתה קובץ בהצלחה (המסמך נוצר ב-DB!) אבל שורת התוצאה "מה זוהה" לא הופיעה על המסך. טסטי הרכיבים עברו; רק ה-E2E גילה את זה.

### סיבת השורש
אחרי קליטה, הרכיב קרא ל-`onRefresh` → `fetchData` של `App` → `setLoading(true)` → `App` מרנדר `return <p>טוען…</p>` — **כל עץ הרכיבים נעקר ומורכב מחדש**, וה-state המקומי (שורות התוצאה) נמחק. בטסטי רכיבים זה לא נתפס כי שם `onRefresh` היה mock ריק.

### הפתרון
`fetchData({ silent: true })` — רענון ברקע שמדלג על `setLoading(true)`. הנתונים מתעדכנים, העץ לא מתפרק, ה-state נשאר.

### הלקח לעתיד
- **רענון-רקע ≠ טעינה ראשונית.** כל `refetch` שנקרא תוך כדי אינטראקציה חייב להיות שקט (בלי מסך טעינה גלובלי שמחליף את העץ).
- טסטי רכיבים עם mocks לא תופסים באגים של אינטגרציית state בין רכיבים — **זה בדיוק התפקיד של E2E.** הפירמידה המלאה הצדיקה את עצמה.

---

## לקח #6 — זיהוי מסמכים מבוסס-כללים לא מספיק למסמכים אמיתיים

**תאריך:** 2026-07-08
**חומרה:** גבוהה (הפיצ'ר המרכזי — הזיהוי — לא עבד בפועל על מסמכי המשתמש)

### התסמין
המשתמש העלה מסמכים אמיתיים ו"לא זוהה כלום" — לא גוף, לא סוג, לא תאריך.

### סיבת השורש
מנוע הסיווג קרא את שכבת הטקסט של ה-PDF וחיפש מילות-מפתח. זה נכשל על:
1. **מסמכים סרוקים/מצולמים** — תמונה בלי שכבת טקסט → אין מה לחפש.
2. **PDF דיגיטלי בעברית** — `pdf-parse` מחזיר עברית הפוכה/משובשת → המילון לא מוצא התאמה.
רשימת מילות-מפתח קבועה גם לא מכסה פורמטים/חברות שלא הוגדרו מראש.

### הפתרון
מנוע **היברידי** (`understand.js`): כללים מקומיים קודם (חינם, מיידי), ונפילה ל-**Claude עם ראייה** (`claude.js`) כשהביטחון נמוך — Claude "רואה" את הדף, קורא סריקות ועברית, ומחזיר `structured output`. בנוסף: התאמת עברית-הפוכה בכללים (חיפוש גם במחרוזת ההפוכה). Claude פעיל רק עם `ANTHROPIC_API_KEY` — בלעדיו נשארים על הכללים (degrade בעדינות).

### הלקח לעתיד
- **MVP מבוסס-כללים ≠ פתרון.** מסמכים אמיתיים = סריקות + עברית + פורמטים מגוונים; לזה צריך מודל ראייה, לא התאמת-מחרוזות.
- **החלטות עלות/פרטיות הן של המשתמש** — כשמשלבים שירות חיצוני בתשלום ששולח מסמכים פיננסיים, לשאול לפני שבונים (נבחר "היברידי").
- **degrade בעדינות** — פיצ'ר שתלוי במפתח API חייב לעבוד (חלקית) גם בלעדיו.

---

## לקח #6 — מונחי סיווג כלליים מדי + דילוג שקט על ה-AI = תמיד אותה תשובה שגויה

**תאריך:** 2026-07-08
**חומרה:** גבוהה (הפיצ'ר המרכזי — זיהוי מסמכים — נראה שבור לחלוטין למשתמש)

### התסמין
כל מסמך שהועלה זוהה כ"מגדל, טופס 867" בביטחון גבוה, מיידית (מאית שנייה), בלי קשר לתוכן. GPT לא רץ בכלל למרות שהוגדר מפתח.

### סיבת השורש (שתי בעיות שהשתלבו)
1. **מונחי זיהוי כלליים מדי:** `'מגדל'` (=בניין/כתובת), `'867'` (=כל סכום כמו "1,867"), `'לאומי'`, `'מיטב'` — מילים עבריות נפוצות שגרמו להתאמות-שווא. מסמך אקראי עם "מגדל" בכתובת + סכום עם 867 → זוהה שקרית כ"מגדל+867".
2. **דילוג שקט על GPT:** המנוע ההיברידי דילג על GPT כש"הכללים בטוחים (high)". התאמת-השווא נתנה ביטחון גבוה מזויף → GPT לעולם לא רץ.

### הפתרון
- **מונחים ספציפיים בלבד:** צירופים ("בנק מזרחי", "מגדל ביטוח") ומספרי טפסים עם הקשר ("טופס 867", לא "867" עירום).
- **כשמוגדר מפתח — GPT רץ תמיד:** לא מדלגים על סמך ביטחון-כללים (שעלול להיות שקרי). הכללים = גיבוי חינמי כשאין מפתח.
- **שקיפות:** חיווי "🤖 זוהה ע\"י GPT / 📋 כללים" לכל מסמך + באנר סטטוס. משתמש רואה מיד אם ה-AI פעיל.

### הלקח לעתיד
- **התאמת substring על מילון קטן היא מלכודת התאמות-שווא.** מונח זיהוי חייב להיות דיסקרימינטיבי — לא מילה שמופיעה בטקסט אקראי.
- **fallback שקט מסתיר כשל.** אם שלב יקר/חכם מדולג — להראmות זאת (method/סטטוס), אחרת "עובד" מסתיר "לא באמת רץ".
- **מהירות חשודה = אבחון.** "מאית שנייה" על פעולה שאמורה לקרוא קובץ ולנתח = משהו לא באמת רץ.

---

## לקח #7 — GPT זיהה נכון אבל שיוך לגוף נכשל: substring גולמי לא מספיק

**תאריך:** 2026-07-08
**חומרה:** בינונית (הזיהוי/תקציר עבד מצוין; רק השיוך לגוף הקיים נכשל בשקט)

### התסמין
GPT ניתח נכון PDF של "וואן זירו" ושל "מזרחי" — תקציר מדויק, סכומים נכונים, תאריך נכון. אבל שני המסמכים נקלטו ל"📥 ממתין לשיוך" במקום להתאים לגוף הקיים במערכת.

### סיבת השורש
`matchEntity()` ב-`understand.js` בדק **הכלה מילולית גולמית** בין `issuerName` שהחזיר GPT (טקסט חופשי, למשל `"ONE ZERO Digital Bank LTD"` או `"Mizrahi-Tefahot Bank"`) לבין השם המדויק שהוזן ב-DB (`"וואן זירו — השקעות"`, `"בנק מזרחי — משפחתי"`). אף אחד מהם לא substring של השני — GPT מתאר את הגוף באנגלית/בניסוח חופשי, בעוד שם הגוף במערכת בעברית עם פורמט (מקף, קטגוריה). ההתאמה נכשלת בלי שגיאה — פשוט `entityId: null`.

### הפתרון
לפני נפילה ל-substring גולמי, לתרגם את שם ה-issuer דרך אותה טביעת-אצבע (`ISSUERS` ב-`classify.js`, מיוצא עכשיו) ששימושה במנוע הכללים: מחפשים איזה `entry.terms` מוכל ב-`issuerName` (או להיפך), ואז מחפשים גוף שה-DB-name שלו מכיל אחד מ-`entry.terms`. כך `"ONE ZERO Digital Bank LTD"` → מזוהה כ-`entry` עם `terms: ['וואן זירו', 'one zero', ...]` → נמצא הגוף שמכיל `"וואן זירו"`. substring גולמי נשאר כ-fallback אחרון (מכסה גופים ייחודיים שהמשתמש הזין ידנית, שאינם ברשימת ה-fingerprints).

### הלקח לעתיד
- **התאמת ישויות בין שני מקורות טקסט חופשי (AI ↔ DB) לא אמינה בלי שכבת נירמול משותפת.** substring/equality גולמי נכשל ברגע שיש הבדל שפה/פורמט/כינוי.
- **להשתמש מחדש בטביעות-האצבע הקיימות כ"מילון תרגום"**, לא רק לזיהוי טקסט חופשי — אותו מבנה נתונים משרת שני צרכים.
- **תוצאה נכונה חלקית (תקציר טוב, שיוך שגוי) מטעה** — קל לפספס שהבעיה בכלל בקומפוננטה אחרת (matching, לא classification/GPT עצמו).

---

## לקח #8 — קובץ הוצמד למסמך, אבל הסטטוס נשאר "ממתין" (ולפעמים אף התאפס ל-NULL)

**תאריך:** 2026-07-08
**חומרה:** בינונית-גבוהה (בלבול משתמש אמיתי — "העליתי את הקובץ, למה זה עדיין ממתין?")

### התסמין
משתמש גורר מסמך של "מגדל" לתיבת הקליטה, המערכת מזהה ומתייקת אותו לסלוט הקיים (הקובץ מוצג, `auto_filed` מתעדכן) — אבל בכרטיס עדיין מופיע תג "⏳ ממתין" אדום, בדיוק כמו לפני ההעלאה.

### סיבת השורש
שני באגים נפרדים שהצטברו לאותה תופעה:
1. **`documents.status` לא היה חלק ממה שמתעדכן בקליטה.** ה-`UPDATE`/`INSERT` ב-`POST /intake` וב-`POST /:id/upload` עדכנו `file_path`, `auto_filed` וכו', אבל אף פעם לא נגעו ב-`status` — כאילו "יש קובץ" ו"הוגש" הם שני דברים לא-קשורים, כשבפועל למשתמש זה אותו דבר.
2. **`PUT /:id/documents` דרס את `status` בלי `COALESCE`** (בניגוד לכל שאר השדות האופציונליים בראוט הזה). `IntakeBox.jsx` (שורת "אשר ושמור") אף פעם לא שולח `status` בגוף הבקשה → הערך מגיע כ-`undefined` → `sanitize()` הופך אותו ל-`null` → ה-`UPDATE` כותב `status = NULL` ל-DB, גם אם הקליטה כבר קבעה `submitted` שורה אחת קודם.

### הפתרון
- בקליטה (`matched`/`create` ב-`/intake`) ובהחלפת קובץ (`/:id/upload`) — לקבוע `status = 'submitted'` במפורש בכל מקום שבו קובץ אמיתי מצטרף למסמך.
- ב-`PUT /:id` — להעביר את `status` ל-`COALESCE(?, status)` כמו כל שאר השדות האופציונליים, כדי ש-PUT חלקי (כמו זה של `IntakeBox`) לא ידרוס בטעות ערך שכבר נקבע.

### הלקח לעתיד
- **שני שדות שמתארים "אותה עובדה" מנקודות מבט שונות (`file_path`/`auto_filed` מול `status`) חייבים להתעדכן ביחד**, אחרת המשתמש רואה מצב לא-עקבי גם כשהקוד "עובד נכון" מבחינה טכנית.
- **כל שדה אופציונלי ב-PUT צריך `COALESCE`, בלי יוצא מן הכלל** — שדה יחיד ששכחו (`status`) הפך לבאג-אפס-שקט (silent null-out) בכל פעם ש-frontend אחר לא שולח אותו. ראה גם `CLAUDE.md` — "פרמטרים: COALESCE-based partial update" כבר מוסכם כדפוס, אבל לא נאכף אוטומטית — צריך לבדוק בעין בכל שדה חדש שמתווסף לראוט.
- **התלונה של המשתמש הייתה הכי אמינה** — "אם העליתי את הקובץ אז העליתי אותו, המשימה הושלמה" היא אינטואיציה נכונה על המודל המנטלי הרצוי; כשה-UI לא תואם אותה, כדאי לחשוד קודם בקוד ולא להסביר את זה כ"ככה זה עובד".

---

## לקח #9 — `lastID` חזר 0 על DB-קובץ, וטסטים עם `:memory:` לא יכלו לתפוס את זה

**תאריך:** 2026-07-09
**חומרה:** גבוהה (פיצ'ר שלם עבד בטסטים ונכשל בשקט בשרת האמיתי)

### התסמין
מחזור המשימות השנתי עבר את כל 126 טסטי ה-backend, אבל ב-E2E (ובשרת אמיתי) הקליטה לא יצרה שום משימה — `rolledTask`/`nextYearTask` חזרו `null` בלי שום שגיאה.

### סיבת השורש
שרשרת של שלושה גורמים:
1. `runQuery` ב-`db/helper.js` קרא את `last_insert_rowid()` **אחרי** `saveDatabase()`. ב-DB מבוסס-קובץ, `saveDatabase` מריץ `db.export()` — שמאפס את `last_insert_rowid` של sql.js ל-0.
2. ב-`:memory:` (כל טסטי ה-backend) `saveDatabase` הוא no-op (`!inMemory`), אז ה-lastID חזר נכון — **הטסטים לא היו מסוגלים עקרונית לתפוס את הבאג.**
3. הקוד החדש היה הראשון שהסתמך על `result.lastID` — כל הקוד הוותיק "במקרה" נמנע ממנו (`SELECT ... ORDER BY id DESC LIMIT 1`), כנראה כי מישהו כבר נכווה ולא תיעד. `getOne(..., [0])` פשוט החזיר `null`, בלי שגיאה.

### הפתרון
להזיז את קריאת `last_insert_rowid()` ב-`runQuery` **לפני** `saveDatabase()`. תוקן במקור (helper.js) ולא בקוד הקורא — כך `lastID` אמין לכולם מעכשיו.

### הלקח לעתיד
- **פער התנהגות בין סביבת הטסט לייצור (`:memory:` בלי export מול קובץ עם export) הוא שטח מת של טסטים** — באג שחי בדיוק בפער הזה יעבור כל טסט. ה-E2E (שרץ על DB-קובץ אמיתי) הוא שתפס אותו — עוד סיבה לפירמידה מלאה.
- **כשקונבנציה עוקפת-משהו חוזרת בקוד ותיק בלי הסבר (`ORDER BY id DESC` במקום lastID) — כנראה יש שם מוקש לא מתועד.** לחקור ולתעד, לא רק לחקות או "לתקן" את הסגנון.
- **כשל שקט (`null` בלי שגיאה) בשרשרת עזר → הפיצ'ר "לא עובד" בלי שום רמז.** ב-flow קריטי עדיף לבדוק `result.lastID` ולהתריע מאשר להמשיך בשקט.

---

## לקח #10 — מחזור "שנתי" שעוגן בשנה הקלנדרית במקום בשנת המסמך

**תאריך:** 2026-07-09
**חומרה:** בינונית-גבוהה (פיצ'ר עבד "בגדול" אבל שגוי סמנטית למסמכים בדוקים/היסטוריים)

### התסמין
המשתמש העלה מסמך מ-2023: נוצר גוף, נוצר תקציר — אבל בעמוד המשימות השנתיות לא נוצרה משימת המשך "לשנה הבאה", ולא הופיע חיווי "הושלם" בשנת המסמך (2023). כאילו המחזור פשוט לא רץ.

### סיבת השורש
1. `applyChecklistCycle` השתמש ב-`const year = new Date().getFullYear()` (2026) לכל השלבים — השלמה, גלגול, ומשימת המשך. מסמך מ-2023 חיפש/יצר משימות סביב 2026/2027, לא 2023/2024. המשתמש שהסתכל על 2023–2024 לא ראה כלום.
2. המחזור רשם השלמה **רק** אם כבר הייתה משימה תואמת בשנה הנוכחית או הקודמת. לגוף חדש בלי היסטוריה — שום משימה מושלמת לא נוצרה, אז "אין חיווי הושלם".

### הפתרון
לעגן את כל המחזור ב-`doc.year` (fallback לשנה הנוכחית אם המסמך בלי שנה): להשלים/לרשום משימה **לשנת המסמך**, ולפתוח משימת המשך ל-`doc.year + 1`. בנוסף — לרשום משימה מושלמת חדשה (`buildCompletedFromDoc`) גם כשאין היסטוריה כלל, כי המסמך עצמו הוא ההוכחה שהחובה בוצעה באותה שנה.

### הלקח לעתיד
- **"שנתי" כמעט תמיד אומר שנת-הנתון, לא שנת-השעון.** בכל לוגיקה סביב תקופות, לשאול מפורשות "איזו שנה — של המסמך או של היום?" — ברירת המחדל `new Date()` היא מלכודת למסמכים היסטוריים/בדוקים.
- **טסטים שכולם משתמשים בשנה הנוכחית לא תופסים עיגון-שנה שגוי.** הבאג הזה חי כי כל הטסטים העלו מסמכים "של השנה"; רק מסמך back-dated חושף אותו. בכל פיצ'ר תלוי-שנה — להוסיף מפורשות מקרה של שנה עבר/עתיד.
- **שינוי סמנטי מפיל טסטים קיימים — וזה בריא.** 2 טסטי E2E נפלו כי הניחו את ההתנהגות הישנה (עיגון-שנה-נוכחית); זה איתות נכון, לא רעש. עדכון הציפיות לסמנטיקה החדשה הוא חלק מהתיקון.

---

## לקח #11 — עדכון-בקליק שנשבר על working-tree "מלוכלך" (בעיקר CRLF ב-Windows)

**תאריך:** 2026-07-09
**חומרה:** בינונית (המשתמש לחץ "בדיקת עדכון תוכנה" וקיבל שגיאה במקום עדכון)

### התסמין
לחיצה על עדכון התוכנה נכשלה: `error: Your local changes to the following files would be overwritten by merge: frontend/src/index.css ... Aborting`. המשתמש לא ערך את הקובץ בכלל.

### סיבת השורש
`update.bat` וכפתור העדכון (`/api/system/update/apply`) הריצו `git pull` על working-tree שאינו נקי. גם קובץ מקור אחד עם שינוי מקומי לא-מחויב עוצר את ה-pull. במחשב Windows הגורם הנפוץ הוא **המרת סופי-שורה (CRLF↔LF)**: בלי `.gitattributes` שמנרמל, git מסמן קבצי טקסט כ"שונו" למרות שהתוכן זהה — וזה שובר כל עדכון.

### הפתרון
1. לפני ה-pull (בשני המקומות) — `git stash push -u` אוטומטי אם ה-tree מלוכלך, כדי שהעדכון "פשוט יעבוד". `finance.db`/`uploads` מוחרגים ב-`.gitignore` ולכן לא נכללים ב-stash — הנתונים בטוחים. התגובה מדווחת אם בוצע stash.
2. `.gitattributes` עם `* text=auto eol=lf` (ו-`*.bat eol=crlf`) — מנרמל סופי-שורה ומונע את ה-churn מלכתחילה.

### הלקח לעתיד
- **כל אוטומציה שמריצה `git pull`/`merge` חייבת להניח working-tree מלוכלך** — לגבות (stash) לפני, לא להניח נקי. משתמש-קצה לא אמור לפתור קונפליקט git ידנית.
- **פרויקט חוצה-פלטפורמות בלי `.gitattributes` = מוקש CRLF.** להוסיף אותו מהיום הראשון, אחרת קבצי מקור "משתנים" לבד ב-Windows.
- **תיקון שנכנס בעדכון עצמו הוא chicken-and-egg** — המשתמש חייב לשחרר את החסימה ידנית פעם אחת (`git restore <file>`), ורק אז העדכון-המוקשח נכנס לתוקף להבא. חשוב לתת גם את הפתרון הידני המיידי וגם את ההקשחה.

---

## תבנית ללקח חדש (העתק-הדבק)

```
## לקח #N — כותרת קצרה

**תאריך:** YYYY-MM-DD
**חומרה:** נמוכה / בינונית / גבוהה

### התסמין
מה נראה/נשבר בפועל (כולל הודעת שגיאה אם יש).

### סיבת השורש
למה זה קרה באמת.

### הפתרון
מה עשינו כדי לפתור, וההשלכות.

### הלקח לעתיד
מה לזכור/לעשות אחרת בפעם הבאה.
```
